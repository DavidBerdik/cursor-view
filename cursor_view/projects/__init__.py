"""Project resolution helpers.

Currently exposes:

- :mod:`cursor_view.projects.inference` for resolving a project from
  workspace metadata, history entries, tree-view state, composer
  ``workspaceIdentifier`` blocks, and URI lists.
- :mod:`cursor_view.projects.git` for the SCM (git repo) fallback.
"""
